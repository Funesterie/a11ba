export type LightEvent = 'spyder.probe' | 'spyder.start' | 'daemon.start' | 'fs.write' | 'fs.test' | 'llm.request';
type LightPayload = Record<string, any>;
/**
 * Allume la lumière blanche sur un évènement.
 * Log + cri dans la corne (si quelqu’un écoute derrière).
 */
export declare function whiteLight(event: LightEvent, payload?: LightPayload): any;
/**
 * Optionnel: petit miroir console pour tout ce qui passe par light.*
 */
export declare function enableGandalfConsoleMirror(): void;
export {};
