"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.whiteLight = whiteLight;
exports.enableGandalfConsoleMirror = enableGandalfConsoleMirror;
// src/core/gandalf-light.ts
const horn_1 = require("./horn");
/**
 * Allume la lumière blanche sur un évènement.
 * Log + cri dans la corne (si quelqu’un écoute derrière).
 */
function whiteLight(event, payload = {}) {
    if (process.env.GANDALF_LIGHT === '0')
        return;
    console.log(`[GANDALF] ✨ ${event}`, payload);
    // On peut brancher un listener avec registerHorn('light.spyder.probe', ...)
    return (0, horn_1.scream)(`light.${event}`, payload);
}
/**
 * Optionnel: petit miroir console pour tout ce qui passe par light.*
 */
function enableGandalfConsoleMirror() {
    // Ici on pourrait faire un registerHorn('light.*', ...) si tu gères les wildcards.
    // Pour l’instant on laisse le hook prêt à être implémenté plus tard.
}
